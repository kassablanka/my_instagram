import React from 'react'
import img from '../loader.svg'

const Loader = () => {
    return (
        <div class="loader">
            <img src={img} alt="Loader" />
        </div>
    )
}

export default Loader